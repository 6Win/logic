# Referência histórica / dados do negócio antigo — NÃO distribuir aos clientes
_nucleo/_origem/

# Backups locais do auto-estudo (rede de segurança da máquina, não produto)
_nucleo/_auto-estudo/backups/*.bak

# Chave de API do usuário — NUNCA subir
_config.json

# Token de sync com o painel online (logic-saas) — NUNCA subir
scripts/sync.config.json

# Cache local da fonte embutida no painel (Artifact) — só bytes de fonte, regenerável
site/.fonte-cache/

# Lixo de sistema
Thumbs.db
.DS_Store
desktop.ini
